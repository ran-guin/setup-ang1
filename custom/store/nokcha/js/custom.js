
$(document).ready(function() {

    // bcg transition

    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h1 = document.getElementsByClassName('header');
          if (h1.length > 0) { h1[0].style.backgroundColor = '#238' }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h2 = document.getElementsByClassName('mini-header');
          if (h2.length > 0) { 
            h2[0].style.backgroundColor = '#238';
            h2[0].style.padding = '5px 50px 5px 100px';
          }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h3 = document.getElementsByClassName('grow-header');
          if (h3.length > 0) {
            h3[0].style.backgroundColor = '#238';
            h3[0].style.padding = '90px';
          }
    }, 0);  

    
    // black-tea 

    setTimeout(function sunrise () {
	// *** final color for header animation *** //
          var h1 = document.getElementsByClassName('black-header');
          if (h1.length > 0) { h1[0].style.backgroundColor = '#542' }
    }, 0);  
    
    setTimeout(function sunrise () {
	// *** final color for header animation *** //
          var h2 = document.getElementsByClassName('mini-black-header');
          if (h2.length > 0) { 
            h2[0].style.backgroundColor = '#542';
            h2[0].style.padding = '5px 50px 5px 100px';
          }
    }, 0);  
    
    setTimeout(function sunrise () {
	// *** final color for header animation *** //
          var h3 = document.getElementsByClassName('grow-black-header');
          if (h3.length > 0) {
            h3[0].style.backgroundColor = '#542';
            h3[0].style.padding = '90px';
          }
    }, 0);  

    // green-tea 
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h1 = document.getElementsByClassName('green-header');
          if (h1.length > 0) { h1[0].style.backgroundColor = '#5a2' }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h2 = document.getElementsByClassName('mini-green-header');
          if (h2.length > 0) { 
            h2[0].style.backgroundColor = '#5a2';
            h2[0].style.padding = '5px 50px 5px 100px';
          }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h3 = document.getElementsByClassName('grow-green-header');
          if (h3.length > 0) {
            h3[0].style.backgroundColor = '#5a2';
            h3[0].style.padding = '90px';
          }
    }, 0);  

    // red-tea 
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h1 = document.getElementsByClassName('red-header');
          if (h1.length > 0) { h1[0].style.backgroundColor = '#842' }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h2 = document.getElementsByClassName('mini-red-header');
          if (h2.length > 0) { 
            h2[0].style.backgroundColor = '#842';
            h2[0].style.padding = '5px 50px 5px 100px';
          }
    }, 0);  
    
    setTimeout(function sunrise () {
  // *** final color for header animation *** //
          var h3 = document.getElementsByClassName('grow-red-header');
          if (h3.length > 0) {
            h3[0].style.backgroundColor = '#842';
            h3[0].style.padding = '90px';
          }
    }, 0);  
        
    $('#searchResults').DataTable();


} );

