div(ng-if="messages && messages.length")
  div.alert.alert-success
    a(href="#" class="close" data-dismiss="alert" aria-label="close") &times;
    ul
      li(ng-repeat="msg in messages")
        i.fa.fa-check
        span &nbsp;
        b {{msg}}
